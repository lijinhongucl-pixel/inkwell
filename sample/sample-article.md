# 示例文章标题

这是一段示例正文，用来验证 inkwell 的排版能力。
重点标记 **粗体强调**，「中文引号」用直角引号。

## 功能列表

- Markdown 转内联样式 HTML
- 图片自动压缩上传
- 公众号兼容性静态校验

## 数据对比

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 镜像大小 | 950MB | 120MB |
| 构建时间 | 180s | 45s |
| 层数 | 18 | 7 |

> 多阶段构建把编译工具留在 builder，运行镜像只含 venv。

```python
# 这是代码块示例
def hello():
    print("inkwell ready")
```

---

结束语。
