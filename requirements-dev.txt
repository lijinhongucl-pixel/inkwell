# 开发与 CI 共用的工具链版本（与 pyproject.toml 的 [dev] extra 保持一致）
#
# 这些下限是「CI 实际安装并通过的版本」，不是拍脑袋写的历史值。
# 之所以抬高：写法过旧（原先 ruff>=0.4 / pytest>=7.4）会给人「支持这些老版本」的
# 错觉，而 CI 因为下限宽松，装的其实一直是最新版 —— 声明与实际不一致。
# 下限应当等于真正验证过的版本；要提高兼容面，应当先扩 CI 矩阵去测。
ruff>=0.16.7
pytest>=9.1.1
pytest-cov>=7.1.0
build>=1.6.1
